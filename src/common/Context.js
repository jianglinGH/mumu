import React from 'react';

const ThemeContext = React.createContext(''); // 创建一个 theme context 
const SearchCondContext = React.createContext(null); // 创建一个 search cond context 
export {ThemeContext, SearchCondContext}

